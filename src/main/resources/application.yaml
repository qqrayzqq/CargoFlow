spring:
  application:
    name: cargoflow

  profiles:
    active: local   # uses application-local.yaml (gitignored)

  # --- PostgreSQL DataSource ---
  datasource:
    url: jdbc:postgresql://localhost:5432/cargoflow
    username: cargoflow
    password: cargoflow
    driver-class-name: org.postgresql.Driver

  # --- jOOQ ---
  jooq:
    sql-dialect: POSTGRES

  # --- Liquibase: runs SQL migrations on app startup ---
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  # --- Elasticsearch ---
  elasticsearch:
    uris: http://localhost:9201

  # --- GraphQL ---
  graphql:
    graphiql:
      enabled: true      # browser UI for testing GraphQL queries
    http:
      path: /graphql

  # --- Spring Docker Compose: auto-starts compose.yaml on app startup ---
  docker:
    compose:
      lifecycle-management: start-and-stop  # start on boot, stop on shutdown

server:
  port: 8080

logging:
  level:
    root: WARN
    com.github.qqrayzqq.cargoflow : DEBUG
    liquibase: INFO
  file:
    name: logs/cargoflow.log

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always

cargoflow:
  geocoding:
    base-url: https://nominatim.openstreetmap.org
  rate-per-kg: 0.01