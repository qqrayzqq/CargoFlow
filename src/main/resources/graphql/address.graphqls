type Address{
    id: ID!
    country: String!
    zip: String!
    city: String!
    street: String!
    buildingNumber: String!
}

extend type Query {
    getAddressById(id: ID!): Address
    searchAddresses(query: String!): [Address]
}
